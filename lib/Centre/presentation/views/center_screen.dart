import 'package:flutter/material.dart';
import '../widgets/center_item.dart';

class center_screen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center_Item(),
    );
  }

}